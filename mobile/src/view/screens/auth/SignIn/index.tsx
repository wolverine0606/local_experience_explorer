import {Text} from 'react-native';

export const SignInScreen = () => {
  return (
    <Text
      style={{
        alignSelf: 'center',
        color: 'white',
        top: 100,
      }}>
      signIn
    </Text>
  );
};
